def bubble_sort(arr):

  for i in range(len(arr)-1, 0, -1):
    for j in range(i):
      if arr[j] > arr[j+1]:
        arr[j], arr[j+1] = arr[j+1], arr[j]
  return arr

lst = [20, 30, 40, 1, 200000, 150, 15, 13]

print(bubble_sort(lst))
